#include "Scene.cpp"
#include <iostream>

using namespace std;

// main function for this project
int main(int argc, char* argv[])
{
    // may need to implement error checkings such as incorrect # of arguments, etc...

    // delcare and initialize a scene object with the given filename;
    Scene scene(argv[1]);
    //scene.initialize();
}
