#include "Vector.h"
#include <iostream>

using namespace std;

Vector::Vector()
{
    // empty for now
}

Vector::Vector(float x_val, float y_val, float z_val)
{
    x = x_val;
    y = y_val;
    z = z_val;
}
